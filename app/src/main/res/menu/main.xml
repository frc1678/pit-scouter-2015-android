<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity" >'

    <item android:id="@+id/realm"
        android:title="Download Realm"
        android:showAsAction="ifRoom" />

    <item android:id="@+id/organizeTeam"
        android:title="Organize Team"
        android:showAsAction="ifRoom" />

    <item android:id="@+id/organizeDbx"
        android:title="Organize All"
        android:showAsAction="ifRoom" />

    <item android:id="@+id/uploadToDbx"
        android:title="Upload Data"
        android:icon="@drawable/cloud"
        android:showAsAction="ifRoom" />

    <item android:id="@+id/action_search"
        android:title="Search"
        android:icon="@drawable/search_icon"
        android:showAsAction="always" />

    <item android:id="@+id/refreshButton"
        android:title="Refresh"
        android:icon="@drawable/refresh_icon"
        android:showAsAction="always" />
</menu>
